 def pipelineA = load "test.groovy"
pipeline {
    agent {label 'LinuxAgent'} 
    
    
    
  stages {
        stage('Conditional') {
            steps { 
                script {
                   pipelineA.stg()
                }
            }
        }
         
  }
}
