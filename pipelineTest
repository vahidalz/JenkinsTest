 #!/usr/bin/env groovy
import hudson.model.*
 def pipelineA = load "test.groovy"

	
pipeline {
    agent {label 'LinuxAgent'} 
    stages{
		 stage('test11') {
				steps { 
					script {
					   pipelineA.stg()
					   
					}
				}
			}
		
	  
	  stage('test') {
				steps { 
					script {
					   echo 'test'
					}
				}
			}
			
			 
			 
	  
  
	}
}
