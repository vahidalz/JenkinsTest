 def pipelineA = load "test.groovy"
pipeline {
    agent {label 'LinuxAgent'} 
    
    
    
  stages {
  stage('test') {
            steps { 
                script {
                   echo 'test'
                }
            }
        }
        stage('Conditional') {
            steps { 
                script {
                   pipelineA.stg()
                }
            }
        }
		 
         
  }
  
}
