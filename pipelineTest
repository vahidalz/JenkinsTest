 def pipelineA = load "test.groovy"

	
pipeline {
    agent {label 'LinuxAgent'} 
    stages{
		 stage('test11') {
				steps { 
					script {
					   pipelineA.stg()
					   
					}
				}
			}
		
	  stages {
	  stage('test') {
				steps { 
					script {
					   echo 'test'
					}
				}
			}
			
			 
			 
	  }
  
	}
}
