 

	
pipeline {
def pipelineA = load "test.groovy"
    agent {label 'LinuxAgent'} 
    stages{
		 stage('test11') {
				steps { 
					script {
					   pipelineA.stg()
					   
					}
				}
			}
		
	  
	  stage('test') {
				steps { 
					script {
					   echo 'test'
					}
				}
			}
			
			 
			 
	  
  
	}
}
