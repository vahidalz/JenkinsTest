 def pipelineA = load "test.groovy"
pipeline {
    agent {label 'LinuxAgent'} 
    
    
    
  stages {
        stage('Conditional') {
            steps { 
                script {
                   pipelineA.stg()
                }
            }
        }
		 stage('test') {
            steps { 
                script {
                   echo 'test'
                }
            }
        }
         
  }
  
}
